<div class="contact-hero">
  <div class="contact-hero__intro">
    <h1 class="contact-hero__title">
      <span>{{ contactIntro.titleUp }}</span>
      <span>{{ contactIntro.titleLow }}</span>
    </h1>
    <h2 class="primary-title contact-hero__primary-title">{{ contactIntro.label }}</h2>
  </div>
  <h3 class="contact-hero__description">{{ contactIntro.description }}</h3>
  <a class="button contact-hero__button button--primary" href="{{ contactIntro.buttonLink }}">
    {{ contactIntro.buttonText }}
  </a>
  <div class="contact-hero__info-wrapper">
    {% for item in contactInfo %}
      <div class="contact-hero__info-item">
        <svg class="svg contact-hero__icon" aria-hidden="true" focusable="false">
          <use href="{{ item.icon }}"></use>
        </svg>
        <p class="contact-hero__text">
          {% if "@" in item.text %}
            <a href="mailto:{{ item.text }}">{{ item.text }}</a>
          {% elif item.text.match('^\\+?[\\d\\s().-]+$') %}
            <a href="tel:{{ item.text | replace(' ', '') | replace('(', '') | replace(')', '') | replace('-', '') }}">{{ item.text }}</a>
          {% else %}
            {{ item.text }}
          {% endif %}
        </p>
      </div>
    {% endfor %}
  </div>
  <div class="contact-hero__map-wrapper">
    <div class="contact-hero__map">
      <picture class="contact-hero__map-img">
        <source srcset="/img/{{ contactMap.img }}-1x.avif 1x, /img/{{ contactMap.img }}-2x.avif 2x" type="image/avif"/>
        <source srcset="/img/{{ contactMap.img }}-1x.webp 1x, /img/{{ contactMap.img }}-2x.webp 2x" type="image/webp"/>
        <img src="/img/{{ contactMap.img }}-2x.webp" alt="{{ contactMap.alt }}" loading="lazy"/>
      </picture>
    </div>
    <div class="contact-hero__map-wrapper-info">
      <svg class="svg contact-hero__map-icon" aria-hidden="true" focusable="false">
        <use href="{{ contactMap.icon }}"></use>
      </svg>
      <div>
        <p class="contact-hero__map-text-upper">{{ contactMap.textPrimary }}</p>
        <p class="contact-hero__map-text-lower">{{ contactMap.textSecondary }}</p>
      </div>
    </div>
  </div>
</div>