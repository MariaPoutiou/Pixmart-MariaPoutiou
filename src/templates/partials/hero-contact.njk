<div class="contact__layout">
  <div class="contact__intro">
    <h2 class="secondary-title contact__secondary-title">{{ contactIntro.label }}</h2>
    <h1 class="contact__title">{{ contactIntro.title }}</h1>
    <h3 class="contact__description">{{ contactIntro.description }}</h3>
  </div>
  <button class="button contact__button button--primary">
    {{ contactIntro.buttonText }}
  </button>
  <div class="contact__info-wrapper">
    {% for item in contactInfo %}
      <div class="contact__info-item">
        <img class="contact__icon" src="{{ item.icon }}" alt="Contact icon"/>
        <p class="contact__text">{{ item.text }}</p>
      </div>
    {% endfor %}
  </div>
</div>
<div class="contact__map-wrapper">
  <iframe class="contact__map" src="{{ contactMap.src }}" height="250" allowfullscreen loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
</div>