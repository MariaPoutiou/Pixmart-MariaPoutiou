<section class="container hero-section hero-section--catalog">
  <h1 class="hero-section__catalog-title">
    {{ heroCatalog.titleStart }}
    <span class="hero-section__catalog-highlight">{{ heroCatalog.titleHighlight }}</span>
    {{ heroCatalog.titleEnd }}
  </h1>
</section>