<section class="container hero-catalog hero-section--catalog">
  <h1 class="herocatalog__title">
    <span>{{ heroCatalog.titleStart }}</span>
    <span class="hero-catalog__highlight">{{ heroCatalog.titleHighlight }}
      {{ heroCatalog.titleEnd }}</span>
  </h1>
</section>