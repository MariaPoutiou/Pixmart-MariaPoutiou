<section class="container hero-catalog">
    <h1 class="hero-catalog__title">
      <span class="hero-catalog__title-start">{{ heroCatalog.titleStart }}</span>
      <span class="hero-catalog__title-end"><strong class="hero-catalog__title-highlight">{{ heroCatalog.titleHighlight }} </strong>{{ heroCatalog.titleEnd }}</span>
    </h1>
</section>