<section class="container hero-section hero-section--catalog">
  <h1 class="hero-section__catalog-title">
    {{ heroCatalog.titleStart }}
    <span class="hero-section__catalog-highlight">{{ heroCatalog.highlight }}</span>
    {{ heroCatalog.titleEnd }}
  </h1>
  <img class="hero-section__catalog-background" src="{{ heroCatalog.background.src }}" alt="{{ heroCatalog.background.alt }}">
</section>