<section class="hero-catalog">
  <div class="container">
    <h1 class="hero-catalog__title">
      <span class="hero-catalog__title-start">{{ heroCatalog.titleStart }}</span>
      <div>
        <span class="hero-catalog__title-highlight">{{ heroCatalog.titleHighlight }}</span>
        <span class="hero-catalog__title-end">{{ heroCatalog.titleEnd }}</span>
      </div>
    </h1>
  </div>
</section>